{
  "name": "prodani-design",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/ensure-assets.mjs",
    "dev": "vite",
    "build": "vite build",
    "setup": "node scripts/build-fonts.mjs && node scripts/embed-images.mjs",
    "build:single": "npm run setup && SINGLEFILE=1 vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "framer-motion": "^11.15.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7",
    "vite-plugin-singlefile": "^2.1.0"
  }
}
